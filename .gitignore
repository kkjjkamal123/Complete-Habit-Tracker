# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Build output / release artifacts (uploaded to GitHub Releases, not committed)
release
release-artifacts
build/icon.png
build/icons
*.apk
*.aab
android/local.properties
android/build
android/app/build
android/.gradle
# Capacitor copies the web build here on every `cap sync` — regenerated, not source.
android/app/src/main/assets/public

# Local environment / Firebase keys & signing material (never commit these)
.env
.env.local
.env.*.local
**/google-services.json
*.keystore
*.jks

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
